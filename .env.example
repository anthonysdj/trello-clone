# Environment Variables for Trello Clone Docker Deployment
# Copy this file to .env and update values as needed

# Frontend Configuration
NODE_ENV=production
NEXT_PUBLIC_API_URL=

# Backend Configuration
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# Database Configuration (H2 In-Memory)
# No configuration needed - using default H2 in-memory database

# Optional: If switching to PostgreSQL in the future
# SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/trello
# SPRING_DATASOURCE_USERNAME=trello
# SPRING_DATASOURCE_PASSWORD=your_password

# Cloudflare Tunnel Configuration
# Get your tunnel token by running: cloudflared tunnel token trello-clone
# Or create a new tunnel: cloudflared tunnel create trello-clone
CLOUDFLARE_TUNNEL_TOKEN=your_tunnel_token_here
